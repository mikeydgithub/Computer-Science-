const isEven = (num) => {
    if (num % 2 === 0) {
        return true;
    }
    else {
        return false;
    }
};

// It always performs the same number of steps. Hence, the time complexity is constant, or O(1).